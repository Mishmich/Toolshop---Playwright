import { test, expect } from '@playwright/test';

test('Complete product order - unauthorized user', async ({ page }) => {

});

test('Complete product order - authorized user', async ({ page }) => {

});